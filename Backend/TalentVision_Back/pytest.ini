[pytest]
DJANGO_SETTINGS_MODULE = TalentVision_Back.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
    --reuse-db
testpaths = tests
env = 
    USE_POSTGRES=False
